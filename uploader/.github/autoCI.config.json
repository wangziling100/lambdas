{
    "actions":[
        "node -v",
        "cd uploader && yarn install",
        "cd uploader && yarn test",
        "cd uploader && rm -r test",
        "sam build",
        "sam local invoke \"UploaderFunction\" -e events/event.json --env-vars env.json",
        "sam deploy"
    ]
}
